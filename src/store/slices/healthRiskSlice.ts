import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const healthRiskSlice = createSlice({
  name: 'healthRisk',
  initialState,
  reducers: {},
});

export default healthRiskSlice.reducer;
