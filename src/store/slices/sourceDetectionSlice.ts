import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const sourceDetectionSlice = createSlice({
  name: 'sourceDetection',
  initialState,
  reducers: {},
});

export default sourceDetectionSlice.reducer;